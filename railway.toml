[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn crypto_trading_desk.api.app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "blockstone-capital"
